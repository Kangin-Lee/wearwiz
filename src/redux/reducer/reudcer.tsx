//state 초기 값 세팅
let initalState = {
  itemCount: 0,
};

function reducer(state = initalState, action = {}) {
  //   if(action.type === "ITEMCOUNT"){
  //       return{...state, }
  //   }
  //   return { ...state };
}

export default reducer;
